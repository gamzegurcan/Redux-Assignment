let createStore;_393‍.x([["increment",()=>increment],["decrement",()=>decrement],["default",()=>_393‍.o]]);_393‍.w("redux",[["createStore",["createStore"],function(v){createStore=v}]]);

const INCREMENT = "INCREMENT";
const DECREMENT = "DECREMENT";

       const increment = (value = 1) => {
  return { //obje
    type: INCREMENT,
    payload: value
  }
}

       const decrement = (value = 1) => ({
  type: DECREMENT,
  payload: value
})

const counterReducer = (counter= 0 , action) => {
  switch(action.type) {
    case INCREMENT:
      return counter + action.payload;
    case DECREMENT:
      return counter - action.payload;
    default:
      return counter;
  }
}

_393‍.d(reducer);